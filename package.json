{
  "name": "fcm-notification-server",
  "version": "1.0.0",
  "description": "Simple Node.js server to send FCM v1 push notifications via Firebase Admin SDK",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.3.1"
  }
}
